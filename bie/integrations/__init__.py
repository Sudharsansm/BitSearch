"""Optional integrations with third-party AI frameworks.

These are thin adapters around BIE's core functions (:func:`bie.websearch`,
:func:`bie.extract`, :func:`bie.crawl_site`) for popular agent frameworks.
Each integration has its own optional dependencies — see the corresponding
submodule's docstring for install instructions.
"""
